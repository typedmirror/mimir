# silent_code_waivers.txt — waivers for the every-code-fires gate
# (tests/scripts/every_code_fires.py). Phase T, track T3a, 2026-07-15.
#
# Format: CODE  justification   ('#' starts a comment; blank lines ignored)
# Every emittable diagnostic code that never fires across the gate corpus
# (mimir check/lint/audit/perf/safety over tests/conformance + tests/smoke,
# check additionally run per-file) must carry a justified waiver here, or
# the gate exits 1. A waiver is a documented debt, not an excuse: coverage
# gaps are T3b fixture candidates, and STALE waivers (code now firing) are
# flagged by the gate for removal.
#
# Categories used below:
#   [other-command]  structurally unreachable via the five gate commands;
#                    fires only under the named command.
#   [coverage-gap]   reachable via a gate command, but no corpus file
#                    triggers the rule today. Fixture candidate for T3b.
#   [crash-blocked]  rule proven to fire, but the measuring run crashes
#                    before emission (pre-existing checker bug, reported at
#                    T3a checkpoint 3). Re-measure after the crash fix.

# ---- [other-command] mimir gpu / compile-gpu (src/gpu/) ----
GPU012   [other-command] emitted only by `mimir gpu`/`compile-gpu` (src/gpu/multigpu.odin); outside gate commands
GPU013   [other-command] emitted only by `mimir gpu`/`compile-gpu` (src/gpu/multigpu.odin); outside gate commands

# ---- [other-command] mimir compile-wasm (src/wasm/) ----
WASM001  [other-command] emitted only by `mimir compile-wasm` (src/wasm/restrictions.odin); outside gate commands
WASM002  [other-command] emitted only by `mimir compile-wasm` (src/wasm/restrictions.odin); outside gate commands
WASM003  [other-command] emitted only by `mimir compile-wasm` (src/wasm/restrictions.odin); outside gate commands
WASM004  [other-command] emitted only by `mimir compile-wasm` (src/wasm/restrictions.odin); outside gate commands
WASM005  [other-command] emitted only by `mimir compile-wasm` (src/wasm/restrictions.odin, add_wasm_diagnostic_severity); outside gate commands
WASM006  [other-command] emitted only by `mimir compile-wasm` (src/wasm/restrictions.odin); outside gate commands
WASM007  [other-command] emitted only by `mimir compile-wasm` (src/wasm/restrictions.odin); outside gate commands
WASM008  [other-command] emitted only by `mimir compile-wasm` (src/wasm/restrictions.odin); outside gate commands

# ---- [other-command] mimir migrate (src/migration/) ----
MIG001   [other-command] emitted only by `mimir migrate` (src/migration/rules.odin union-syntax); outside gate commands
MIG002   [other-command] emitted only by `mimir migrate` (src/migration/rules.odin); outside gate commands
MIG003   [other-command] emitted only by `mimir migrate` (src/migration/rules.odin); outside gate commands
MIG004   [other-command] emitted only by `mimir migrate` (src/migration/rules.odin); outside gate commands
MIG005   [other-command] emitted only by `mimir migrate` (src/migration/rules.odin); outside gate commands
MIG006   [other-command] emitted only by `mimir migrate` (src/migration/rules.odin); outside gate commands
MIG007   [other-command] emitted only by `mimir migrate` (src/migration/rules.odin); outside gate commands
MIG008   [other-command] emitted only by `mimir migrate` (src/migration/rules.odin); outside gate commands

# ---- [other-command] mimir lsp (src/lsp/) ----
E000     [other-command] emitted only by the LSP server (src/lsp/analysis.odin) for in-editor parse errors; outside gate commands

# ---- [crash-blocked] ----
# (empty — CONC001 waiver removed 2026-07-15 at Phase T integration: the T5
#  recursion-guard fixes eliminated the SIGSEGV and the code now fires, per
#  the independent verifier's stale-waiver flag. Doctrine: stale = removed.)

# ---- [coverage-gap] reachable via `mimir check` (checker domain passes) ----
DB002    [coverage-gap] src/checker/infer.odin (db query result schema); no corpus file triggers it; T3b fixture candidate
JSON002  [coverage-gap] src/checker/infer.odin (json schema type); no corpus file triggers it; T3b fixture candidate
JSON003  [coverage-gap] src/checker/json_check.odin; no corpus file triggers it; T3b fixture candidate
M001     [coverage-gap] circular-import diagnostic (src/modules/graph.odin); corpus has no project-mode import cycle; T3b fixture candidate
# (SHAPE001-003 waivers removed 2026-07-15 at Phase T integration: the codes
#  now fire across the corpus — unblocked by the T5 crash fixes — per the
#  independent verifier's stale-waiver flag.)
TEST001  [coverage-gap] src/checker/test_check.odin (pytest mock rule); no corpus file triggers it; T3b fixture candidate

# ---- [coverage-gap] reachable via `mimir lint` ----

# ---- [coverage-gap] reachable via `mimir perf` ----

# ---- [coverage-gap] reachable via `mimir safety` ----
DATA002  [coverage-gap] FIRES under conform (virtual-only path, fixture gap_data002.py marker-matched) but SILENT under CLI check — registry-reinit family 4th instance (init_virtual_registry double-init reassigns data singleton Type_IDs; same mechanism blocks DB002/JSON002/JSON003). Wave-2 structural fix unparks all four. Ruled by lead at Wave C close, 2026-07-17.
API005   [coverage-gap] fixture withdrawn at Wave C close: the author's openapi.json overwrote/collided with the PRE-EXISTING tracked corpus openapi.json (which api_contract.py's API001/API002 depend on); lead's cleanup then briefly deleted the tracked file (restored). Re-land needs a fixture designed against the existing corpus json or an isolated dir. Lead ruling 2026-07-17, corrected same day.
